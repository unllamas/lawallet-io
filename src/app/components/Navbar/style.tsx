'use client';

import { styled } from 'styled-components';

export const NavbarPrimitive = styled.nav`
  display: flex;
  justify-content: center;
  height: 60px;

  > div {
    justify-content: center;
  }
`;
